# NOTE: keep this file pure ASCII (see requirements.txt for why).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.3.4
# FastAPI's TestClient is built on httpx.
httpx==0.28.1
# Load testing (docs/REQUIREMENTS.md 8.5)
locust==2.32.5
