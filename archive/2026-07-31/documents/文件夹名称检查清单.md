# 文件夹名称检查清单

## 📋 需要更新的位置

根据搜索结果，以下位置包含项目名称，需要根据新的文件夹名称进行更新：

### 1. Maven配置
- **文件**: `pom.xml`
- **位置**: 
  - `<artifactId>lottery-engine</artifactId>` (第9行)
  - jar包名称会基于artifactId生成：`lottery-engine-1.0.0.jar`

### 2. 应用配置
- **文件**: `src/main/resources/application.yml`
- **位置**: 
  - `spring.application.name: lottery-engine` (第8行和第62行)

### 3. 文档文件
- **文件**: `README.md`
  - 项目结构示例中的 `lottery-engine/` (第24行)
  - jar包名称 `lottery-engine-1.0.0.jar` (第102行)

- **文件**: `QUICK_START.md`
  - jar包名称 `lottery-engine-1.0.0.jar` (第91行)

- **文件**: `运行说明.md`
  - jar包名称 `lottery-engine-1.0.0.jar` (第32行)

- **文件**: `start.bat`
  - jar包名称 `lottery-engine-1.0.0.jar` (第29行)

### 4. 编译输出
- **位置**: `target/` 目录下的jar包名称会自动基于artifactId生成

---

## 🔄 更新说明

**注意**: 
- `artifactId` 通常使用小写字母和连字符（kebab-case），如：`lottery-engine`
- 文件夹名称可以使用下划线（snake_case），如：`lottery_engine`
- 两者可以不同，但建议保持一致

**请告诉我新的文件夹名称，我将更新所有相关引用。**

